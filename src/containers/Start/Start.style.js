import {StyleSheet} from "react-native";

export default StyleSheet.create({
    stepContainer: {
        width: '100%',
        height: '100%',
        resizeMode: 'stretch',
        marginTop: 110
    }
});